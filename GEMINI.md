# OneNext Agent Guide

Use `AGENTS.md` as the shared project instructions.

Key rules:

- Do not add behavior that has not been discussed.
- Backend API responses must match `docs/prd.md`.
- Use `.agents/skills/pr-workflow/SKILL.md` for commit and PR checks.
